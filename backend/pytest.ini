[pytest]
markers =
    unit: fast, isolated stage/unit tests, no external model downloads
    integration: end-to-end pipeline tests using synthetic fixtures
    concurrency: job queue / GPU semaphore race-condition tests
asyncio_mode = auto
